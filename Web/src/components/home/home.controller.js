export default class HomeController {
    constructor() {
        this.posts = '';
    }
    getLatest() {
        // var servCall = LatestService.getPosts();
        // servCall.then(function (d) {
            // this.posts = JSON.parse(JSON.stringify(d));
            this.posts="Testing";
        // }
        // )
    }
}